# Privacy

Reports are local. Upgrade Guard performs no telemetry and no external API calls during scans. Text and JSON output redact URL credentials, home-directory usernames, common token/password assignments, proxy credentials and IPv4 addresses by default.

Users should still review exported reports before sharing them because package and repository names can reveal local system context.
