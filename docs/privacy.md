# Privacy

Reports are local. Upgrade Guard performs no telemetry and no external API calls during scans. JSON export redacts credentials in repository URLs and home-directory path prefixes.

Users should still review exported reports before sharing them because package and repository names can reveal local system context.
